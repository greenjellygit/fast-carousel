import { FastCarouselDefDirective } from './fast-carousel-def.directive';

describe('FastCarouselDefDirective', () => {
  it('should create an instance', () => {
    const directive = new FastCarouselDefDirective();
    expect(directive).toBeTruthy();
  });
});
